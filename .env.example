### Plantilla .env.example para Entrevista TTS App
# Copia este archivo a `.env` y rellena los valores reales. No comitees tu `.env` con claves privadas.

# Clave de API de OpenAI (obligatoria)
OPENAI_API_KEY=""

# (Opcional) URL base de OpenAI si usas Azure o un proxy compatible
OPENAI_API_BASE=""

# Modelos por defecto (puedes dejar vacíos para usar los valores por defecto del backend)
OPENAI_MODEL_LLM=
OPENAI_MODEL_TTS=

# Voces por defecto
DEFAULT_VOICE_Q=
DEFAULT_VOICE_R=

# Backend
BASE_URL=http://localhost
PORT=

# Frontend (Vite)
VITE_API_URL=http://localhost:8000
VITE_PORT=
VITE_HOST=0.0.0.0

# Opciones para detectar cambios en macOS / contenedores
CHOKIDAR_USEPOLLING=true
CHOKIDAR_INTERVAL=100

# Notas:
# - Rellena solamente los valores necesarios. Mantén la clave real en tu archivo local `.env`.
# - Si usas Docker Compose, el servicio lee `.env` en la raíz del repo.
# - Para establecer variables temporalmente en macOS/Linux:
#   export OPENAI_API_KEY="sk-..."
